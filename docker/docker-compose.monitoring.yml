services:

  prometheus:

    image: prom/prometheus:v3.5.0

    container_name: cloudwatchx-prometheus

    ports:
      - "9090:9090"

    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml

    restart: unless-stopped